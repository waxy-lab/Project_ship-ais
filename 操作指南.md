source .venv/bin/activate
激活py虚拟环境
source install/setup.bash
激活工作空间
socat -d -d pty,raw,echo=0 pty,raw,echo=0
打开两个通信端口
python3 -m ais_simulator.ais_sim_node --ros-args -p output_port:=/dev/pts/7
激活ais模拟器
ros2 run ais_to_mqtt ais_reader --ros-args -p port:=/dev/pts/9
激活读取ais数据脚本
ros2 run ais_to_mqtt analyzeData
ros2 run ais_to_mqtt mqtt_bridge